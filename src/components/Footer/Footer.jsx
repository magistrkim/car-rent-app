import { StyledFooter, Title } from './Footer.styled';
import Socials from '../Socials/Socials';

const Footer = () => {
  return (
    <StyledFooter>
      <Title>Folow us on social media</Title>
      <Socials />
    </StyledFooter>
  );
};

export default Footer;
